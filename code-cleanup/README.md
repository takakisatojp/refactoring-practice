# Code Cleanup（コードクリーンアップ）

不要なコード、冗長な実装を整理し、シンプルで保守性の高いコードを目指します。

## 📚 このトピックで学べること

- ✅ 不要なコードの削除
- ✅ デッドコードの除去
- ✅ インポートの整理
- ✅ マジックナンバーの定数化
- ✅ コード重複の削減

## 📂 問題一覧

| # | 問題 | 難易度 | 説明 |
|---|------|--------|------|
| 01 | [Dead Code](./01-dead-code/) | ⭐ Easy | 未使用の関数・変数を削除 |
| 02 | [Unused Imports](./02-unused-imports/) | ⭐ Easy | 不要なインポートの整理 |
| 03 | [Magic Number](./03-magic-number/) | ⭐ Easy | マジックナンバーを定数化 |

## 🎯 推奨学習順

すべて基礎レベルです。順番に学びましょう：

1. 01: Dead Code - 未使用コードの削除
2. 02: Unused Imports - インポート整理
3. 03: Magic Number - 定数化

## 💡 学習ポイント

### デッドコード
実行されないコードを削除することで、保守性を向上。

```typescript
// ❌ デッドコード
const oldFormatter = (value: string) => {
  // もう使われていない
};

// ✅ 削除
```

### インポート整理
```typescript
// ❌ 不要なインポート
import { unused, helper } from './utils';
const result = helper(data);

// ✅ 必要なものだけ
import { helper } from './utils';
const result = helper(data);
```

### マジックナンバー
```typescript
// ❌ マジックナンバー
if (age > 18) { /* ... */ }
if (count > 100) { /* ... */ }

// ✅ 定数化
const ADULT_AGE = 18;
const MAX_ITEMS = 100;

if (age > ADULT_AGE) { /* ... */ }
if (count > MAX_ITEMS) { /* ... */ }
```

---

**ルートREADME**: [../README.md](../README.md)
